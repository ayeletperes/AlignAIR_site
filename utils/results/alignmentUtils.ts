// components/results/utils/alignmentUtils.ts

/**
 * splitSequence moved to utils/alignment/splitSequence.ts - import from there if needed
 */

/**
 * GetSequenceMismatchIdx moved to utils/alignment/mismatchUtils.ts - import from there if needed
 * (this version had different parameter names but same functionality)
 */
